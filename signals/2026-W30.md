# Signals — week 2026-W30

Raw daily capture. One line per signal. Internal — input for the weekly
digest and the guide-refresh pass. Not rendered on the site.

## 2026-07-20

- [Anthropic: "Anthropic runs large-scale code migrations with Claude Code"](https://claude.com/blog/ai-code-migration) — first-party writeup of two real migrations (Bun's 1M-line Zig→Rust port in under 2 weeks for ~$165k in tokens; a 165k-line Python→TypeScript port in one weekend) with a concrete six-step workflow: rulebook + dependency map, stress-test the rules on mini-migrations, parallel per-file translation, a compiler-error fixing loop, smoke testing, then behavior-matching; adversarial reviewers disagree-and-escalate-to-a-third, a serialized build daemon avoids redundant recompiles, and every phase is resumable by checking disk state rather than session memory — richer than guide §05's one-line "migration → dynamic workflow" mention (subagents/workflows · workflow · practice-candidate)
- [Simon Willison: Claude Code's JS runtime is now Bun, and Bun itself has been quietly rewritten in Rust](https://simonwillison.net/2026/Jul/19/claude-code-in-bun-in-rust/) — verified by extracting 563 embedded Rust source filenames and a canary Bun 1.4.0 build from the Claude Code binary (change traces back to v2.1.181, June 17); the maintainer's own framing was "startup got 10% faster on Linux, otherwise barely anyone noticed" — 497-point HN thread, pure infrastructure trivia with no user-facing setting to change (meta · discussion)
- [Fable 5's plan-included window ends on schedule; usage reverts July 20 to prepaid credits ($10/$50 per Mtok, no grace period)](https://support.claude.com/en/articles/15424964-claude-fable-5-on-your-plan) — resolves the multi-week watch item from 07-18/07-19: the 50%-weekly-limit boost keeps extending (now through Aug 19) but Fable 5's free-with-plan access reverted to usage credits as announced for Pro plans and Team/Enterprise standard seats — Max plans and Team/Enterprise premium seats keep the 50%-cap inclusion; enable usage credits under Settings → Usage before reaching for Fable 5 today if you're on a standard seat (models · feature)
- [Show HN: Shikigami — runs multiple AI coding agents (including Claude Code) in parallel, each isolated in its own git worktree](https://shikigami.dev/) — third-party wrapper around the same worktree-isolation pattern Claude Code's own `isolation: 'worktree'` subagents use natively; modest traction (6 pts), worth a glance if you're scripting parallel agents outside the built-in workflow tool (subagents/workflows · discussion)

## 2026-07-22

- [Claude Code changelog: v2.1.217](https://code.claude.com/docs/en/changelog) — adds a default 20-concurrent-subagent cap (override `CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS`) so one message can't fan out unbounded background agents, and flips nested subagent spawning to **off by default** (override `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH` to allow depth again) — this is a session-wide cap separate from the workflow tool's own per-script concurrency limit (min(16, CPU cores − 2), still in force) that guide §05 already documented; also fixes `--max-budget-usd` not actually stopping already-running background subagents once the cap is hit (subagents/workflows · feature · practice-candidate) [guide §05 patched]
- [Claude Code Desktop: iOS Simulator pane](https://code.claude.com/docs/en/desktop-ios-simulator) — public beta (Pro/Max/Team, not Enterprise) opens Apple's iOS Simulator in a pane next to the conversation whenever Claude builds/runs/checks an iOS app; Claude drives the simulator directly (no computer-use/Accessibility permissions needed), you can tap alongside it, up to 4 devices per session, org policy can disable via `disableMobileSimulatorTools`; covered same day by MacRumors/9to5Mac/TestingCatalog — first genuinely new mac-app surface since Artifacts, and guide §06 doesn't mention it yet (apps · feature · practice-candidate)
- [Claude Code changelog: v2.1.216](https://code.claude.com/docs/en/changelog) — adds `sandbox.filesystem.disabled` setting to skip filesystem isolation, and fixes a quadratic message-normalization cost that was slowing down long sessions (context · feature)
- [Simon Willison: fireside chat with Cat and Thariq from the Claude Code team](https://simonwillison.net/2026/Jul/21/cat-and-thariq/) — team says Claude Code's system prompt shrank ~80% moving to Fable-class models by removing examples and restrictive "do not" instructions in favor of trusting model judgment; frames dynamic workflows as "Claude prompting Claude all the way down"; auto mode risk from prompt injection/exfiltration is now claimed lower than the average human reviewer after "thousands of evals" since January (meta · discussion)
- [Claude Code changelog: v2.1.217](https://code.claude.com/docs/en/changelog) — login-expiry warning now fires 3 days before expiry (was 5), and the frontend-design plugin's suggestion tip is capped at 3 lifetime impressions — small UX trims, no decision changed (meta · tip)

## 2026-07-23

- [Claude Code changelog: v2.1.218](https://code.claude.com/docs/en/changelog) — `/code-review` now runs as a background subagent instead of filling the conversation, and keeps stacked slash-command output as its review target — extends 2.1.215's "explicit-invoke only" fix with a second improvement to the same skill in one week (subagents/workflows · feature · practice-candidate)
- [Claude Code changelog: v2.1.218](https://code.claude.com/docs/en/changelog) — `/deep-research` now only starts when explicitly invoked; Claude no longer launches it on its own — same shape as 2.1.215's fix for `/verify`/`/code-review`, closing the same silent-autonomy gap for a third skill (subagents/workflows · feature · practice-candidate)
- [Claude Code changelog: v2.1.218](https://code.claude.com/docs/en/changelog) — auto mode's dangerous-`rm`, background-`&`, and suspicious-Windows-path checks no longer open a permission dialog — the auto-mode classifier adjudicates them directly now; same release stops plan-mode-with-auto from prompting for Bash commands its static analyzer can't prove read-only, handing that judgment to the classifier too — a further shift of trust from dialogs to the classifier itself, relevant to the open "what auto mode actually does" question (permission modes · feature · practice-candidate)
- [Claude Code changelog: v2.1.218](https://code.claude.com/docs/en/changelog) — skills with `context: fork` now run in the background by default (opt out per-skill with `background: false` in frontmatter) — brings forked skills in line with subagents' background-by-default behavior since 2.1.198 (skills/plugins/mcp/hooks · feature · practice-candidate)
- [Claude Code changelog: v2.1.218](https://code.claude.com/docs/en/changelog) — fixes agent-frontmatter hooks running from untrusted folders: a hook now requires the agent file's own folder to have accepted workspace trust first — closes a gap where a repo could smuggle a hook in via an agent definition outside the trusted root (skills/plugins/mcp/hooks · feature)
- [Claude Code changelog: v2.1.218](https://code.claude.com/docs/en/changelog) — server-managed settings: benign feature/cost toggles no longer trigger the settings-approval prompt — narrows what a managed-settings push interrupts you for, same trust-surface tuning theme as recent weeks (teams · feature)
- [Claude Code changelog: v2.1.218](https://code.claude.com/docs/en/changelog) — a large accessibility pass: screen-reader announcements for word/line deletions, VoiceOver no longer misreads trailing spaces as "new line", and plugin/settings panels now move the terminal cursor so screen readers/magnifiers can follow arrow-key navigation in `--ax-screen-reader` mode (meta · feature)

## 2026-07-24

- [Anthropic: fast mode for Claude Opus 4.7](https://code.claude.com/docs/en/fast-mode) — deprecated since June 25, removed on schedule today: `/fast` requests on Opus 4.7 now error instead of silently running at standard speed (no auto-fallback, unlike Opus 4.6's behavior); the 4.7 model itself is unaffected at standard speed, migrate to Opus 4.8 ($10/$50 per Mtok fast-mode pricing vs 4.7's $30/$150) to keep the speedup (models · feature · practice-candidate) [guide §01 patched]
- No new Claude Code release since v2.1.218 (July 22) as of this sweep — changelog, GitHub releases, and the repo's CHANGELOG.md all still show 2.1.218 as latest; quietest 24h stretch since the 2.1.20x run started in early July (meta · discussion)
- [Show HN: Hibernate and restore Claude Code sessions across reboots](https://github.com/SteveVitali/claude-hibernate) — snapshots running sessions (directory, session ID, flags) before shutdown and relaunches them after reboot with no terminal wrapper or pre-setup required; modest traction (6 pts) but directly on-theme with the "supervised background run" thread — a practitioner filling the gap Claude Code itself doesn't cover (session survival across a host reboot, not just a network blip) (subagents/workflows · discussion)
