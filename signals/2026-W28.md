# Signals — week 2026-W28

Raw daily capture. One line per signal. Internal — input for the weekly
digest and the guide-refresh pass. Not rendered on the site.

## 2026-07-06

- [Claude Code 2.1.200 — AskUserQuestion no longer auto-continues](https://code.claude.com/docs/en/changelog) — the ~60s AFK timeout that let Claude guess a default on an unanswered question during long runs is off by default; opt back in via `/config` (feature · modes)
- [Artifacts open to Pro and Max](https://code.claude.com/docs/en/artifacts) — publishing session work as a private live claude.ai page is no longer Team/Enterprise-only; on Pro/Max it stays private to you (feature · apps)
- [Let Claude pick its own subagent models](https://simonwillison.net/2026/Jul/3/judgement/) — Simon Willison relays a Claude Code team one-liner: send routine coding to a subagent on a lower-power model, keep judgment/review in the lead; more done, Fable allowance drained slower (workflow · models)

## 2026-07-07

- [Claude Code 2.1.202 — Dynamic workflow size setting](https://code.claude.com/docs/en/workflows) — new `/config` knob (unrestricted/small/medium/large) nudges Claude toward fewer agents when writing a workflow; advice to the model, not an enforced cap — 16 concurrent / 1,000 per run hard limits still apply (feature · workflow)

## 2026-07-08

- [Claude Code 2.1.203 — switching agents no longer restarts their work](https://github.com/anthropics/claude-code/releases/tag/v2.1.203) — returning to the `claude agents` list used to silently stop and re-run a running subagent from scratch; fixed. Same release fixes a 15–20s stall opening/switching background sessions on macOS (feature · apps)

## 2026-07-09

- [Claude Code 2.1.205 — `/doctor` is now a full setup checkup](https://code.claude.com/docs/en/changelog) — `/doctor` diagnoses and fixes setup issues, with `/checkup` as its alias; same release fixes `--json-schema` silently emitting unstructured output on an invalid schema and no longer rejects schemas using the `format` keyword (apps · feature)
- [2.1.205 hardens auto mode for unattended runs](https://code.claude.com/docs/en/changelog) — auto mode now blocks tampering with session transcript files and asks before running `rm -rf` on an unresolved variable; background-task notifications now state that no human input occurred, so a run can't fabricate a transcript approval (modes · feature)
- [2.1.205 — auto-update streams the binary to disk](https://code.claude.com/docs/en/changelog) — update downloads no longer buffer in memory, cutting ~400 MB of peak memory (apps · feature)
- [2.1.205 reserves the "Claude Browser" MCP name](https://code.claude.com/docs/en/changelog) — reserved alongside "Claude Preview" ahead of a Desktop pane rename — a heads-up that the desktop browser pane is being renamed (apps · feature)
- [Claude Code 2.1.204 — SessionStart hooks stream in headless sessions](https://github.com/anthropics/claude-code/releases/tag/v2.1.204) — fixes hook events not streaming during `SessionStart` hooks in headless runs, which could get remote workers idle-reaped mid-hook (skills/plugins/mcp/hooks · feature)
- [China issues 'backdoor' security alert over Anthropic's Claude Code](https://www.reuters.com/legal/litigation/china-issues-backdoor-security-alert-over-anthropics-claude-code-2026-07-08/) — a nation-state security notice naming the tool; not a product change, but it shapes the ecosystem the reader works in (meta · discussion)
- [How to Kill the Bloat in Claude Code's System Prompt](https://www.aihero.dev/how-to-kill-the-bloat-in-claude-codes-system-prompt) — practitioner piece on trimming context overhead to reclaim budget for long runs (context · tip)

## 2026-07-10

- [Claude Code 2.1.206 — background agents upgrade themselves right after you update](https://code.claude.com/docs/en/changelog) — previously a background session paid a slow stale-version upgrade the moment you attached; now it upgrades in the background immediately post-update. Same release makes `EnterWorktree` ask for confirmation before entering a git worktree outside `.claude/worktrees/` (subagents/workflows · feature)
- [2.1.206 — `/doctor` now flags bloated checked-in `CLAUDE.md` files](https://code.claude.com/docs/en/changelog) — new check proposes trimming content Claude could just derive from the codebase; same release makes `/commit-push-pr` auto-allow `git push` to `remote.pushDefault` (or the sole remote), not just `origin` (context · feature)
- [2.1.206 fixes MCP per-server timeouts and background-worker env overrides](https://code.claude.com/docs/en/changelog) — `request_timeout_ms` set via `--mcp-config`/`.mcp.json` was being ignored (long MCP calls timed out at 60s in fresh sessions); `CLAUDE_CODE_EXTRA_BODY` was silently dropped by `claude agents`/`--bg` workers, now follows the dispatching session (skills/plugins/mcp/hooks · feature)
- [Anthropic responds to China's 'backdoor' warning on Claude Code](https://www.scmp.com/news/china/article/3359901/anthropic-hits-back-after-china-warns-claude-code-backdoor-risks) — follow-up to 07-09's alert: Anthropic says Chinese users "were not supposed to be using it in the first place"; China's National Vulnerability Database had flagged versions 2.1.91–2.1.196 specifically over a "built-in monitoring mechanism" it says can transmit data without consent — Anthropic has acknowledged embedding anti-distillation tracking, framed as security, not a backdoor (meta · discussion)
- [Reportedly: the "Fable 5 included in your weekly limit" banner vanished early](https://news.ycombinator.com/item?id=48852172) — small Ask HN thread (6 points) claims the promo messaging disappeared from Claude Code before the July 12 free-window deadline it advertised; unconfirmed in any changelog — watch before treating as fact (models · discussion)
- [VS Code 1.128 adds multi-chat Claude agent sessions](https://code.visualstudio.com/updates/v1_128) — released 07-08: a single Claude session can now hold multiple peer chats with independent history/model, fork from an earlier turn, and run turns concurrently; terminal/IDE-side, not the macOS/iOS app, but signals where multi-agent UX is heading (subagents/workflows · feature)

## 2026-07-11

- [Claude Code 2.1.207 — auto mode goes default-on for Bedrock, Vertex AI, and Foundry](https://github.com/anthropics/claude-code/releases/tag/v2.1.207) — no longer needs the `CLAUDE_CODE_ENABLE_AUTO_MODE` env opt-in on those platforms; disable via `disableAutoMode` in settings if you don't want it. Same release moves the `autoMode` toggle out of repo-resident `.claude/settings.local.json` into `~/.claude/settings.json` only — a repo can no longer flip auto mode on for you (modes · feature · practice-candidate)
- [2.1.207 — Bedrock, Vertex, and Claude Platform on AWS default to Opus 4.8](https://code.claude.com/docs/en/changelog) — was a mix of defaults before; every AWS-hosted path now opens on the heavy-lifting model unless you pick otherwise (models · feature · practice-candidate)
- [2.1.207 closes a consent-dialog gap and a plugin shell-injection hole](https://github.com/anthropics/claude-code/releases/tag/v2.1.207) — non-interactive runs (`claude -p`, the SDK) were silently recording remote managed settings as consented without ever showing the security dialog; separately, `${user_config.*}` in shell-form plugin hooks/monitors/MCP `headersHelper` commands is now rejected — use exec-form `args` or `$CLAUDE_PLUGIN_OPTION_<KEY>` instead (skills/plugins/mcp/hooks · feature)
- [2.1.207 fixes terminal freezing on long streamed output](https://code.claude.com/docs/en/changelog) — keystrokes were lagging or dropping while a response streamed a very long list, table, or code block; also fixes the transcript jumping above the answer's start once streaming finished (apps · feature)
- [Claude Fable 5 sweeps the art/style rounds, Opus 4.8 is inconsistent, in a 12-model build-off](https://www.tryai.dev/blog/gpt-5.6-build-off-12-models) — Fable went 5/5 on the Rubik's-cube and calculator tasks and swept the SVG bonus rounds while Opus went 0/5 on the cube; both lagged GPT-5.6 Sol and Grok 4.5 on the 3D raycaster maze — 141-point HN thread, useful data point for the "Fable is the ceiling, not the default" call (models · discussion)
- [Two Show-HN entries chase Claude Code remote/persistent-session control](https://github.com/thrinz/agentpeek) — `agentpeek` (drive a persistent session as terminal or chat) and [`hasharot`](https://github.com/Mamasodikov/hasharot) (control an agent from Telegram); both under 5 points but continue the community itch the built-in Remote Control feature is aimed at (subagents/workflows · discussion)

## 2026-07-12

- [Claude Code's 50% weekly-limit boost reverts July 13, 6 PM PDT](https://x.com/ClaudeDevs/status/2054639777685934564) — the May-13 bump for Pro/Max/Team/seat-based Enterprise ends tomorrow evening with no extension announced; corroborated by multiple trackers (apidog, chatforest) quoting the original Anthropic post, not yet on a claude.com page — front-load anything usage-heavy today if you're near the ceiling; may be the same deadline behind yesterday's vanished "Fable 5 in your weekly limit" banner (models · discussion · practice-candidate)
- [2.1.207 also fixes Remote Control reliability](https://code.claude.com/docs/en/changelog) — task-status updates were getting lost when a dropped connection recovered, and desktop-hosted sessions weren't showing background agent/workflow progress on mobile or web; both fixed in the same release as yesterday's auto-mode/Opus-default changes (apps · feature)
- [2.1.207 fixes an agent-teams mailbox crash loop](https://code.claude.com/docs/en/changelog) — a malformed teammate mailbox message triggered a repeating error every second until someone manually deleted the mailbox file; same release also stops auto-update from silently overwriting a custom launcher script or symlink at `~/.local/bin/claude` (`/doctor` now flags an externally-managed launcher) (subagents/workflows · feature)
- [2.1.207 stops reading plugin option values from repo-committed settings](https://code.claude.com/docs/en/changelog) — `pluginConfigs` now only comes from user, `--settings`, or managed settings, not project-level `.claude/settings.json`; continues the same-release pattern of pulling trust-sensitive config out of repo-resident files (see `autoMode`'s move to `~/.claude/settings.json` in the same release) (skills/plugins/mcp/hooks · feature)
