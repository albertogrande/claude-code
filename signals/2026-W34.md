# Signals — week 2026-W34

Raw daily capture. One line per signal. Internal — input for the weekly
digest and the guide-refresh pass. Not rendered on the site.

## 2026-08-18

Catch-up sweep — no scout ran 08-09 through 08-17. Prioritizing the highest-value facts from that gap plus today's changelog state; the weekly digest should treat this as thin, not exhaustive, coverage of ten missed days.

- [Auto mode is now the default in Claude Code for Pro, Max, and Team plans](https://claude.com/blog/auto-mode-default-in-claude-code) — **shipped on schedule 2026-08-14**: new sessions on Pro/Max/Team default to auto instead of manual. Per the [permission-modes docs](https://code.claude.com/docs/en/permission-modes), this isn't tied to one version number — it's a server-side flag gated by client version: **v2.1.228+ on macOS/Linux/WSL, v2.1.233+ on native Windows** (2.1.228 shipped Aug 11, three days ahead of the flip). Enterprise/API/Bedrock/Vertex/Foundry stay opt-in, targeted "within a month"; a pre-existing personal default persists, org-managed defaults untouched. **Correction on sourcing**: [Simon Willison](https://simonwillison.net/2026/Aug/8/auto-mode/) (Aug 8) and [TechCrunch](https://techcrunch.com/2026/08/09/anthropic-is-turning-claude-codes-auto-mode-on-by-default/) (Aug 9) covered the *announcement* ahead of ship, not same-day as previously logged here; only [9to5Mac](https://9to5mac.com/2026/08/14/psa-claude-code-enabling-auto-mode-as-default-next-week-anthropic-says/) (Aug 14) is same-day — **guide §02 patched today**, had no default stated (modes · feature · practice-candidate)
- [Claude Code changelog: v2.1.233](https://code.claude.com/docs/en/changelog) (Aug 14, same release date as the auto-mode default flip) — quietly ships a second default-flip the blog post didn't headline: **task-tracking tools (TaskCreate/Get/Update/List, TodoWrite) are now off by default on Opus 4.8, Sonnet 5, Fable 5, Mythos 5, and newer models** (`CLAUDE_CODE_ENABLE_TODO_TOOLS=1` restores them) — guide §05/§07 don't mention TodoWrite today so nothing to disprove, but worth the weekly checking whether visible todo lists in the UI change (subagents/workflows · feature · practice-candidate)
- Same v2.1.233: Windows auto mode regression from 2.1.232 (stopping for manual approval on ordinary `cd && cmd > file` Bash) fixed same-day (modes · feature)
- [Claude Code changelog: v2.1.232](https://code.claude.com/docs/en/changelog) (Aug 13) — **subagent forking is now on by default**: a `fork` subagent inherits the full conversation + prompt cache, and non-teammate agent spawns in interactive sessions now run in the background by default; also ships `@name` in-prompt mentions that route through `SendMessage` to reach another session directly; Fable 5 restored as a `/advisor` option for orgs with Fable access (**corrected**: earlier draft of this note misattributed the Fable 5 item to 2.1.233 — it's a 2.1.232 item) — the forking/background changes extend the background-by-default subagent thread guide §05 already covers, not yet reflected there (subagents/workflows · feature · practice-candidate)
- [Claude Code changelog: v2.1.234](https://code.claude.com/docs/en/changelog) (Aug 17, latest as of this sweep) — session auto-continues when a claude.ai usage limit resets (toggle in `/config`); Windows NT-namespace (`\??\`) path rejection extended to remote file reads, session restore, CLAUDE.md includes, workflow scripts, and file uploads, closing the remaining pre-approval NTLM credential-leak surface from the same bypass class as 2.1.232's fix; GitLab MR badges land in the footer/statusline (meta/modes · feature)
- [auto mode temporarily unavailable, Shift+Tab won't cycle to it](https://github.com/anthropics/claude-code/issues/33587) — reports auto mode unreachable via toggle or `defaultMode` setting, briefly appearing on startup then reverting to "Exited Auto Mode." **Checked and downgraded**: filed 2026-03-12 against v2.1.74 — five months before this week's 2.1.232–234 and long before the auto-default rollout existed — and the issue is now **closed**, with no visible maintainer resolution comment. Unrelated to this week's default-flip; logged only in case the same symptom resurfaces (modes · discussion)
- status.claude.com not swept in detail this pass (10-day gap too wide to reconstruct reliably) — next scout should resume the daily incident read fresh rather than try to backfill (meta · discussion)

## 2026-08-19

- [Claude Code changelog: v2.1.235](https://code.claude.com/docs/en/changelog) (Aug 18) — fixes a real permission-dialog bug: **Shift+Tab inside the permission prompt's comment field was approving the edit and granting session-wide edit permission** instead of closing the field; also tightens "don't ask again" so its display text always matches what the grant would actually cover, and withholds the option when contents can't be fully shown (modes · feature)
- status.claude.com: Claude Opus 5 and Claude Haiku 4.5 degraded (elevated errors) 09:42–11:02 UTC 08-19, ~80 min, resolved same window — separately, multi-model degraded performance (Opus 5, Sonnet 5, Fable 5, Mythos 5, Haiku 4.5) ran 16:20–19:01 UTC 08-18, Opus 5 specifically 16:11–18:23 UTC — reliability wobbles continue across the whole gen-5 line, consistent with the running thread (meta · discussion)
- [Claude Code changelog: v2.1.236](https://code.claude.com/docs/en/changelog) (Aug 19) — three auto-mode hardening items in one release: `Monitor` allow rules are now set aside while auto mode is active so Monitor commands get the same classifier review as Bash; Bedrock/Vertex/Foundry (and telemetry-disabled) auto mode now uses the same classifier defaults as the Claude API, including severity-scored classification; and the git-status clean-tree check can no longer be fooled by a repo's `status.showUntrackedFiles=no` setting — same bypass-closing pattern as the trust-hardening thread all month (modes · feature · practice-candidate)
- Same v2.1.236: new `ANTHROPIC_DEFAULT_MODEL` env var sets the model new sessions start on (a `/model` pick still overrides and persists, unlike `ANTHROPIC_MODEL`); `notify_when_idle` added to cross-session `SendMessage` — ask another local session to ping once when it next goes idle, opt-in, one-shot, no polling (macOS/Linux); macOS sandbox wildcard read-deny rules (e.g. `**/.env`) now take precedence inside allowed read regions and can't be bypassed by renaming the denied file (models · subagents/workflows · feature)
- [Opus 5.0 drives incoherence into the stratosphere](https://github.com/anthropics/claude-code/issues/77136) (176 pts HN) — despite the title, the body's complaints are mostly about **Opus 4.8's** writing style (jargon-heavy, forced metaphors, argumentative tone, made-up aphorisms); no maintainer response yet; adjacent to the running Opus-5-vs-4.8 preference debate but not itself an Opus 5 defect report — flagged, not confirmed (models · discussion)
- [Turning websites into micro CLIs for Claude Code to save on tokens](https://github.com/only-cli/oc) (4 pts HN) — wraps web-fetch-heavy sites as thin CLIs so Claude reads structured text instead of full HTML, aimed directly at the 07-31 finding that 63% of session tokens go to full webpage fetches (context · tip)

## 2026-08-20

- [Claude Code changelog: v2.1.237](https://code.claude.com/docs/en/changelog) (Aug 20, today) — fixes prompt caching for sessions on an LLM gateway or custom base URL (same bug family as 07-16's Bedrock/Vertex/Mantle/Foundry caching fix); adds a built-in **"Concise" output style** — Claude leads with results and skips preamble/narration while doing the same work, selectable under Output style in `/config` (context · feature · practice-candidate)

## 2026-08-21

- [Claude Code changelog: v2.1.238](https://code.claude.com/docs/en/changelog) (Aug 20, 20:33 UTC — released hours after 2.1.237 same day) — the release's standout fix for the harness-overhead thread: **unbounded memory growth in long interactive sessions is fixed** — subagent tool results are now released once they scroll out of the recent display window, instead of accumulating for the life of the session (context · feature · practice-candidate)
- Same v2.1.238: plugin marketplaces/MCP gain **`headersHelper`** — a command that mints short-lived HTTP headers (e.g. a token) for catalog/archive fetches and MCP connections; requires the folder's trust dialog accepted first and runs without inherited credential env vars, and `claude plugin install/update` now prompt `[y/N]` before running one — a new trust-relevant extensibility primitive, not yet in guide §04 (skills/plugins/mcp/hooks · feature)
- Same v2.1.238: cross-session `SendMessage` no longer fails silently — sending to a session with `crossSessionInbound: "refuse"` now reports "refused," and a rate-limited/full inbox now tells the sender instead of the message vanishing, closing a gap in the thread since the 08-08 cross-session-messaging feature (subagents/workflows · feature)
- Same v2.1.238: `claude self-hosted-runner` gains `--defer-shutdown-max-min` (keep serving attached sessions on SIGTERM before parking) and `--proxy-authorization-command`/`--proxy-authorization-file` for egress proxies needing a freshly minted header per connection (subagents/workflows · feature)
- Same v2.1.238: new `keybindingFlavor: "readline"` setting (Ctrl+W deletes to previous whitespace, Bash-style); the double-press-Ctrl+L/Cmd+K `/clear` shortcut is **removed** — both keys now just repaint, fixing accidental `/clear` loops in 1-row nvim terminals (apps · feature)
- [Structural census of 445,348 published Claude Code skills/subagents/plugins/marketplaces](https://toolproof.kynth.studio/census) — static (no-execution) checks find **9.70% fail to load** (43,199 listings); subagents fail at 21.9% (2.8× skills' 7.89%, since `name`/`description` are mandatory with no fallback), plugins only 0.16%; **88.4% of all failures are one cause** — malformed YAML frontmatter that doesn't parse; full failing dataset published CC BY 4.0 — concrete "validate your skill/subagent frontmatter" tip for anyone publishing to a marketplace (skills/plugins/mcp/hooks · tip · practice-candidate)
- status.claude.com: "Elevated errors on requests to multiple models" incident, rated **Major** impact, affected claude.ai, Claude API, **Claude Code**, and Claude Cowork 19:16–19:42 UTC today (26 min, resolved) — no specific models named; separately, a Minor "Elevated errors on Google connectors" incident (Sheets/Docs/Slides/Chat, 18:32–19:01 UTC) affected Claude's Google integrations, not Claude Code itself (meta · discussion)

## 2026-08-22

- [Claude Code changelog: v2.1.239](https://code.claude.com/docs/en/changelog) (Aug 21) — largest release since 2.1.238, mostly fixes: **cost estimates now factor in the 1.1× US-only-inference premium for data-residency workspaces** (previously understated the true cost for those orgs); adds a `/claude-api upgrade` skill that migrates a Python project from the `anthropic` SDK's 0.x to 1.x line; cloud-session plugins synced from claude.ai now display as `name@synced` (cost-control · subagents/workflows · feature)
- Same v2.1.239: fixes **WebFetch retaining expired page content in memory for the entire session instead of clearing it after its stated 15-minute cache** — same host-process-memory pathology as 2.1.238's subagent-tool-result leak fixed yesterday, not the token-billing overhead the 07-13/07-31 measurements track, but another instance of "the harness itself wasn't bounded" (context · feature)
- Same v2.1.239: fixes sessions whose title starts with `/` being unaddressable by cross-session `SendMessage` (closes a rough edge in the thread since 08-08), masked password-style input fields allowing pasted text to leak back out elsewhere, and custom session titles disappearing after ~64 KB of conversation; Alpine/musl builds gain working native image-paste, clipboard, and audio-capture add-ons (subagents/workflows · apps · feature)
- status.claude.com: quiet since 08-21's single brief 26-min blip — no new incidents through 08-22 so far (meta · discussion)
- [Quick impressions: a week of using Codex more than Claude](https://allaboutcoding.ghinda.com/a-week-of-using-codex-more-than-claude/) (86 pts HN) — practitioner switched daily driver for a week and reports Claude over-engineers (adds abstractions/type aliases unasked) and over-helps (guesses and does more than requested); on git, it's Codex that struggled — producing nested branch targets and a 4,000+ line rebase mishap — while the author says Claude "could understand my intention to branch out from other work and keep them in sync"; author concedes some of the preference is familiarity bias, not a controlled comparison (subagents/workflows · discussion)
- [System reminders: how Claude Code steers itself](https://michaellivs.com/blog/system-reminders-steering-agents/) — reverse-engineered (not Anthropic-sourced) catalog of the 37 `<system-reminder>` nudges Claude Code injects at lifecycle events (file-state changes, context pressure, consecutive tool failures) to steer behavior without hard rules; cites the community [claude-code-system-prompts](https://github.com/Piebald-AI/claude-code-system-prompts) extraction repo as its source — flagged as unofficial, useful context for why the harness "nags" about things like re-reading a file before editing (context · meta · discussion)

## 2026-08-23

- [Claude Code changelog: v2.1.239 full notes](https://code.claude.com/docs/en/changelog) (Aug 21) — closer read past yesterday's headline items surfaces misses worth logging: **Windows now has cross-session messaging** (`SendMessage`/`ListAgents` across machines), matching macOS/Linux since 2.1.224/08-07; `CLAUDE_CODE_RETRY_WATCHDOG` persistent-retry mode now fails immediately on an org spend-limit or out-of-credits error instead of waiting indefinitely for a reset (cost-control tie-in); `/goal` background check-ins now back off (30 min → 1 h → every 2 h after that) instead of repeating every 30 minutes, and resuming a session via the `--resume` picker now restores its active goal; `ListAgents`/`/list-agents` now list live teammates, not just subagents and other sessions (subagents/workflows · feature)
- [Claude Code releases: v2.1.240](https://github.com/anthropics/claude-code/releases) (Aug 22, 14:45 UTC) and **v2.1.241** (Aug 23, 00:52 UTC) — both shipped with only "Bug fixes and reliability improvements," no itemized notes published at sweep time — same quiet-release pattern as v2.1.220's stretch in late July (meta · discussion)
- [Anthropic appears to be A/B testing reduced effort levels in Claude Code](https://news.ycombinator.com/item?id=49401549) (178 pts/163 comments HN, source: [X/@argofowl](https://twitter.com/argofowl/status/2091150597374537729)) — reportedly a server-side experiment enrolls Fable 5 sessions on Claude Code 2.1.236+ into a shrunk effort scale where "high" now behaves like the old "low," while older client versions and Opus 5 sessions are untouched; not in any changelog. **Anthropic responded in-thread**: Claude Code's Thariq says it's a serving-config test that only changes the displayed numeric value ("the scale isn't 0-100 ... the effort you selected is the effort you're getting"), cites evals showing no performance change, and is offering credits for confirmed regressions reported via `/feedback`. Anthropic's own July 7 effort-levels blog post (checked today) predates this and says nothing about it. Echoes the March 4→April 7 default-effort walk-back the guide already knows about — the display change is confirmed, the "reduced effort" framing is disputed by Anthropic (models · discussion)
- [SkillWorks](https://skillworks.kynth.studio) — new companion census tool from the same publisher (Kynth Studios) as yesterday's toolproof.kynth.studio skill/subagent audit: nightly-refreshed scoring of 503,570+ skill/subagent/plugin/marketplace listings on load-validity (40%), maintenance recency (25%), adoption (20%), and doc quality (15%), publishing the full broken-listing set rather than hiding it — same "validate your frontmatter before publishing" practice point as yesterday, now with an ongoing scoreboard instead of a one-time static census (skills/plugins/mcp/hooks · tip)
- status.claude.com: fully quiet Aug 21–23, no incidents logged — third straight clean day since 08-20's last incident (a 26-min elevated-errors blip on multiple models closing 19:42 UTC), the longest quiet stretch since the Aug 6–11 gap that preceded the Aug 12–20 run of near-daily incidents (meta · discussion)
