# Signals — week 2026-W29

Raw daily capture. One line per signal. Internal — input for the weekly
digest and the guide-refresh pass. Not rendered on the site.

## 2026-07-13

- [Anthropic extends the 50% Claude Code weekly-limit boost — and Fable 5's plan-included access — through July 19, 2026, 11:59:59 PM PT](https://support.claude.com/en/articles/15910845-claude-code-may-july-2026-weekly-limits-promotion) — Fable 5's access window's second extension (July 7 → July 12 → July 19), now folded into the same date as the main boost; resolves 07-12's "reverts tonight" watch — it didn't. Covers Pro/Max/Team/seat-based Enterprise, all platforms (CLI, IDE, desktop, web); 5-hour limits unaffected by this promotion (models · feature · practice-candidate)
- [Fable 5's free window still has a hard edge: usage credits at $10/$50 per Mtok once it lapses](https://fable5.app/fable-5-usage-limits/) — double Opus 4.8's $5/$25 rate; multiple trackers quote Anthropic's subscriber email warning of no grace period and no automatic fallback model if credits aren't funded before the window closes — now July 19 given the extension above (models · feature · practice-candidate)
- [HN (571 pts): Claude Code sends ~33k tokens of overhead before it reads your prompt, vs OpenCode's ~7k](https://systima.ai/blog/claude-code-vs-opencode-token-overhead) — logging-proxy comparison on a fresh, MCP-free session: ~6.5k-token system prompt + ~24k tool schemas (27 tools) vs OpenCode's ~2k + ~4.8k (10 tools); direct evidence for the context-budget habit of trimming what you don't need before a long run (context · tip)
- [Practitioner report: a stray "court" token precedes hung tool calls on long Opus 4.8 sessions](https://lilting.ch/en/articles/claude-code-court-tool-call-hang) — claims tool-call serialization breaks down mid-session, emitting raw `<invoke>`-style text the harness can't execute, before any MCP server ever sees the request; a single practitioner blog post, not a vote-tracked community thread — the underlying glitch is corroborated by several open GitHub issues (#64108, #65705, #65791, #68354) but remains unfixed and unconfirmed in any changelog — flag, don't treat as settled yet (apps · discussion)
- [Reportedly: a "Claude Honeycomb EAP" model briefly surfaced inside Cursor, fueling Opus 5 speculation](https://thewincentral.com/claude-opus-5-leak-1m-context-window-launch/) — community theory reads a 1M-context leak as an accidental Opus 5 preview; no Anthropic channel confirms Honeycomb or Opus 5 exists — unverified, watch only (models · discussion)

## 2026-07-14

- [2.1.208: screen reader mode, vim insert-mode remaps, corporate-launcher wrapper support](https://code.claude.com/docs/en/changelog) — `claude --ax-screen-reader` / `CLAUDE_AX_SCREEN_READER=1` / `"axScreenReader": true` gives NVDA/JAWS users plain-text rendering (matches the ask in long-open [#11002](https://github.com/anthropics/claude-code/issues/11002), which is still open as of publish — this release doesn't appear to have closed it); `vimInsertModeRemaps` maps two-key sequences like `jj` to Escape in vim mode; `CLAUDE_CODE_PROCESS_WRAPPER` lets agent view and the background service run through required corporate launchers (apps · feature · practice-candidate)
- [2.1.208 also caches MCP tool-pool assembly: up to 7x faster tool rounds at high tool counts](https://code.claude.com/docs/en/changelog) — cuts per-tool-call CPU overhead in print/SDK sessions with many MCP tools, plus a large batch of memory-leak fixes (MCP stdio stderr, LSP documents, async hook output) and a fix for context window briefly resetting to 200k after CLI auto-updates — partial relief on the harness-overhead thread's CPU side, not the ~24k-token schema cost itself (context · subagents/workflows · feature)
- [2.1.209 reverts an overly broad guard that blocked `/model` and other dialogs inside `claude agents` background sessions](https://github.com/anthropics/claude-code/releases) — same-day follow-up fix, ships hours after 2.1.208 (subagents/workflows · feature)
- [Microsoft field study of its own Claude Code + GitHub Copilot CLI rollout: adopters merged ~24% more PRs over a 4-month window](https://arxiv.org/abs/2607.01418) — first developer-telemetry study of agentic CLI adoption at enterprise scale; found uptake spread through social networks and retention tracked with engineers' existing coding activity more than demographics; [HN discussion, 53 pts](https://news.ycombinator.com/item?id=48899321) (subagents/workflows · discussion)

## 2026-07-15

- [2.1.210 fixes `isolation: 'worktree'` subagents being able to run git-mutating commands against the main repo checkout instead of their own isolated worktree](https://code.claude.com/docs/en/changelog) — closes an isolation escape in the exact primitive dynamic workflows use to fan out agents without collision; same release hardens the Agent tool against indirect prompt injection via content a subagent reads (subagents/workflows · feature · practice-candidate)
- [2.1.210 fixes the `ultracode` keyword opt-in firing on non-human-originated input — webhook payloads, relayed PR comments](https://code.claude.com/docs/en/changelog) — closes a gap where an external actor, not a person, could trigger a large-scale dynamic workflow; same release fixes `claude agents --effort ultracode` silently failing to reach dispatched sessions (subagents/workflows · feature · practice-candidate)
- [2.1.210 adds a startup warning for permission rules written against `Write(path)`, `NotebookEdit(path)`, or `Glob(path)` — use `Edit(path)`/`Read(path)` instead](https://code.claude.com/docs/en/changelog) — those tools don't honor path-scoped rules the way Edit/Read do, so a rule written that way was silently matching nothing; guide's permission-mode table doesn't cover rule syntax yet (permission modes · feature · practice-candidate)
- [2.1.210: auto mode's background permission classifier now defaults to Sonnet 5 for external sessions, validated on the session's first request and pinned for its duration](https://code.claude.com/docs/en/changelog) — first confirmation of which model screens auto-mode actions outside Anthropic-hosted sessions (modes · feature)
- [2.1.210: Fable temporarily shows unavailable in the advisor picker while a server-side issue causing Fable advisor failures is fixed](https://code.claude.com/docs/en/changelog) — same-day, self-reported outage, distinct from the July 19 promotion-window story; watch for resolution (models · discussion)
- [2.1.210 fixes `claude attach` failing with "job not found"/"agent is still starting" during session transitions, and killed background sessions leaving permanent `git worktree lock`s behind](https://code.claude.com/docs/en/changelog) — two more entries in the ongoing background-session reliability push (apps · feature)
- [2.1.210: memory writes that leave a `MEMORY.md` index over its read limit now produce an explicit error instead of silently truncating](https://code.claude.com/docs/en/changelog) — directly relevant to anyone maintaining a `MEMORY.md` (including this desk's own editorial memory) close to the line (context · feature · practice-candidate)
- Community sweep (HN new stories, Anthropic newsroom, Simon Willison, WebSearch) turned up nothing beyond the usual low-vote Show HN posts and older Anthropic product news (Claude for Teachers, Canada research commitment) — no new practitioner threads worth flagging today

## 2026-07-16

- [2.1.211 fixes auto mode overriding a PreToolUse hook's `ask` decision for unsandboxed Bash — a hook `ask` now floors the decision at a prompt](https://code.claude.com/docs/en/changelog) — closes a real gap in the "what actually gets screened under auto mode" story: a deterministic hook guardrail could previously be silently downgraded by the background classifier (permission modes · feature · practice-candidate)
- [2.1.211 changes "always allow" permission rules to save at the repository root, so approvals granted in a git worktree persist across sessions and worktrees](https://code.claude.com/docs/en/changelog) — previously an approval given inside one worktree didn't carry to a sibling worktree on the same repo; direct payoff for anyone running parallel worktree sessions (permission modes · subagents/workflows · feature · practice-candidate)
- [2.1.211 improves background agent result reporting — Claude now reports the status of still-running agents and waits for the real completion instead of fabricating results](https://code.claude.com/docs/en/changelog) — continues the same-week trust-in-background-work push (07-15's worktree git-isolation and injection-hardening fixes); this one closes a gap where an impatient status check could get a made-up answer (subagents/workflows · feature · practice-candidate)
- [2.1.211 fixes a prompt-caching regression on Bedrock, Vertex, Mantle, and Foundry that billed the trailing system-context block as fresh input tokens on every request](https://code.claude.com/docs/en/changelog) — a silent cost bug on non-Anthropic-hosted endpoints, same shape as the one 2.1.181 fixed for custom `ANTHROPIC_BASE_URL`/Foundry setups; relevant to the cost-control thread for anyone on cloud-hosted billing (models · feature)
- [2.1.211 fixes `/clear` not resetting the session cost counter — the statusline's cost now starts at $0 after `/clear`](https://code.claude.com/docs/en/changelog) — the statusline was silently carrying over spend from before a context clear, understating what a fresh context actually costs you (context · feature · practice-candidate)
- [2.1.211 hardens permission previews relayed to chat channels against bidirectional-override, zero-width, and look-alike quote characters, so a tool input can no longer visually alter the approval message shown to a reviewer](https://code.claude.com/docs/en/changelog) — a spoofing vector in remote-approval flows (Slack/Teams-style relays), closed same week as the Agent tool's indirect-prompt-injection hardening (07-15) (teams · feature)
- [2.1.211 adds `--forward-subagent-text` / `CLAUDE_CODE_FORWARD_SUBAGENT_TEXT` to include subagent text and thinking in stream-json output](https://code.claude.com/docs/en/changelog) — useful for anyone building tooling on top of headless/SDK sessions that fan out subagents and want visibility into what they're actually doing, not just the final summary (subagents/workflows · feature)
- [HN (31 pts): "Agentty" ships as a drop-in alternative to claude-code](https://github.com/1ay1/agentty) — third-party CLI positioning itself as compatible with Claude Code's config/skills surface; unverified how deep the compatibility runs, watch for practitioner reports (meta · discussion)

## 2026-07-17

- [2.1.212 redesigns `/fork`: now copies a conversation into a new background session (its own row in `claude agents`) while you keep working; the old in-session-subagent behavior is now `/subtask`](https://code.claude.com/docs/en/changelog) — splits "branch off a parallel approach" (fork, background) from "delegate a piece of this task" (subtask, in-session); biggest workflow-shape change of the week (subagents/workflows · feature · practice-candidate)
- [2.1.212 adds session-wide hard caps: subagent spawns capped at 200/session (`CLAUDE_CODE_MAX_SUBAGENTS_PER_SESSION`, reset by `/clear`), WebSearch calls capped at 200/session (`CLAUDE_CODE_MAX_WEB_SEARCHES_PER_SESSION`)](https://code.claude.com/docs/en/changelog) — a second, session-wide backstop on top of the existing 16-concurrent/1,000-per-run runtime caps; guide §05 said the runtime caps were "the only real backstop" — patched (subagents/workflows · feature · practice-candidate)
- [2.1.212 auto-backgrounds any MCP tool call running longer than 2 minutes (`CLAUDE_CODE_MCP_AUTO_BACKGROUND_MS`)](https://code.claude.com/docs/en/changelog) — a slow MCP call no longer blocks the session; same release also cuts inter-agent token usage by removing `SendMessage` body duplication — direct, if partial, relief on the harness-overhead thread (context · subagents/workflows · feature)
- [2.1.212 fixes plan mode auto-running file-modifying Bash commands without a permission prompt](https://code.claude.com/docs/en/changelog) — closes a real, long-documented gap (practitioner writeup ["Claude Code's Plan Mode Isn't Read-Only, But You Can Fix It"](https://blog.sondera.ai/p/claude-codes-plan-mode-isnt-read), GH issue #6716, closed as not planned) where plan mode's "no disk writes" guarantee didn't hold for Bash specifically (permission modes · feature · practice-candidate)
- [2.1.212: `/resume` now opens a picker of past sessions — including deleted ones — and reopens the pick as a background session](https://code.claude.com/docs/en/changelog) — same release deprecates the Task tool's `mode` parameter; subagents now just inherit the parent session's permissions instead of taking their own mode (apps · subagents/workflows · feature)
- [2.1.212 fixes worktree creation following a repo-committed symlink at `.claude/worktrees`](https://code.claude.com/docs/en/changelog) — a repo could plant a symlink there to redirect a worktree session outside its sandbox; closes the same class of isolation gap as 07-15's `isolation: 'worktree'` fix, same week (subagents/workflows · feature · practice-candidate)
- [2.1.212 adds `claude auto-mode reset` (with a confirmation prompt, `--yes` to skip) to restore default auto-mode settings](https://code.claude.com/docs/en/changelog) — a one-command undo for anyone who's been tuning auto-mode's trust scope and wants back to stock (modes · feature)
