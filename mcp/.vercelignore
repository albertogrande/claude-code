# Vercel only needs the serverless function (api/mcp.js) and what it imports
# (src/server.js, src/data.js). Exclude the standalone Node entry, tests, and
# docs so Vercel doesn't treat them as build entrypoints.
src/index.js
test.mjs
test-vercel.mjs
Dockerfile
README.md
